{{- $secrets := .Values.secrets | default dict -}}
{{- $centops := $secrets.centops | default dict -}}

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "centops.fullname" . }}-appsettings-secrets
  labels:
    {{- include "centops.labels" . | nindent 4 }}
type: Opaque
data:
#Add secrets here
 cosmosAccount: {{ default "" .Values.cosmosAccount | b64enc | quote }}
 cosmosKey: {{ default "" .Values.cosmosKey | b64enc | quote }}